/**
 * 内核: 页面 显示
 */

module.exports = app => {


    class Viewer
    {
        constructor(){
            this.ctx = null;
        }

        // 继承自
        inherit(){};

    }

    // 返回
    app.Viewer = Viewer;
}